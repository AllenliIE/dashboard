<header>
  <nav class="navbar navbar-expand-lg fixed-top navbar-light bg-light px-4">
    <div class="container-fluid">
      <a class="navbar-brand text-primary" href="/">
        <i class="fa-solid fa-industry"></i>
        Dashboard
      </a>
      {{#if user}}
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse"
        aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarCollapse">
        <ul class="navbar-nav ms-auto mb-2 mb-md-0">
          <li class="nav-item d-inline-flex align-items-center">
            <span class="text-primary nav-link">Hello!{{ user.name }}</span>
          </li>
          {{#if user.isAdmin}}
          <li class="nav-item">
            <a href="/admin" class="nav-link text-primary">
              <i class="fa-solid fa-screwdriver-wrench"></i>
            </a>
          </li>
          {{/if}}
          <li class="nav-item">
            <a href="/logout"><button class="btn text-primary">
                <i class="fa-solid fa-right-from-bracket"></i>
              </button></a>
          </li>
        </ul>
      </div>
      {{/if}}
    </div>
  </nav>
</header>